export declare const formatPhoneNumber: (phoneNumber: string) => string;
/**
 * would throw an error if the provided number is invalid
 * @param phoneNumber a valid mobile number
 */
export declare const validatePhoneNumber: (phoneNumber: string, strict?: boolean | undefined) => boolean;
